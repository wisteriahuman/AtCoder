#include <bits/stdc++.h>
using namespace std;
using ll=long long;

int main() {
	ll N;
	vector<ll> A(N + 1, 0);
	vector<ll> B(N + 1, 0);
	for (int i = 1; i <= N; i++) {
		cin >> A[i] >> B[i];
	}

	return 0;
}
